<h1>listing all todos</h1>

<p>
<%= link_to "create pf", new_pf_path %>
 </p>
<table>
<tr>
<th>
Name
</th>
<th>
Pfamount
</th>
</tr>
<% @pfs.each do |pf| %>
<tr>
<td><%= pf.name %></td>
<td><%= pf.pfamount %></td>
<td><%= link_to "Edit", edit_pf_path(pf) %></td>
<td><%= link_to "Show", pf_path(pf) %></td>
<td><%= link_to "Delete", pf_path(pf), method: :delete, data: {confirm: " are you sure"} %></td>

</tr>
<% end %>
</table>